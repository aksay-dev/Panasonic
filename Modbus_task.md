Я сильно упрощаю задачу.
Необходимо реализовать сохранение параметров последовательного интерфейса MODBUS в NVS памяти.
При следующей перезагрзке (RESET) программа берет параметры связи из NVS памяти.
Для того, чтобы изменить параметры последовательного интерфейса MODBUS теперь не нужно пересоздавать контроллер.
Необходимость в регистре MB_HOLDING_APPLY_MODBUS_SETTINGS также отпадает.
Необходимо вернуть обработку записи в hoding регистры через API компонента MODBUS.
